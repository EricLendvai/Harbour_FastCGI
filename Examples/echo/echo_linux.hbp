-hbexe
#-inc
-workdir=build/lin64/${HB_COMPILER}/${BuildMode}/hbmk2/

-obuild/lin64/${HB_COMPILER}/${BuildMode}/${EXEName}.exe

echo.prg

#When using devcontainer use .. (parent folder), before the project root does not work. /src/Harbour_FastCGI is a mounted folder.
#../../hb_fcgi/hb_fcgi_linux.hbm

/src/Harbour_FastCGI/hb_fcgi/hb_fcgi_linux.hbm

